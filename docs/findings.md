# Findings

Populated post-experiment (PRD §9, §10). A negative result with clean methodology
and a calibrated judge is a publishable contribution — write it up as such, do not
bury it.

## Status

Not yet run. Pending: frozen cost model, dataset access, decision run + judge, the
100-pair judge spot-check.

## Template (fill after the decision run)

- Headline numbers (link `results/latest/headline_numbers.json`).
- H5 gate result and what it implies about the substitutability premise.
- Control gaps (subject vs unfiltered / scrambled).
- Decision per the §4 matrix and the action taken.
- Threats to validity actually observed (filter bias magnitude, noise fraction,
  judge/human agreement, WildChat representativeness).
